---
const config = {
  title: "Ready to transform your workflow?",
  description: "Join +500 companies already building with our core technology.",
  ctaText: "Get Started Now",
  ctaHref: "/contact",
  secondaryText: "View Documentation",
  secondaryHref: "/docs"
};
---

<section class="relative py-20 px-4 transition-colors duration-500">
  <div class="mx-auto max-w-5xl rounded-3xl bg-mono-black dark:bg-mono-white p-8 md:p-16 text-center shadow-2xl overflow-hidden relative">
    <div class="absolute top-0 right-0 -mt-8 -mr-8 h-32 w-32 rounded-full bg-accent/20 blur-3xl"></div>
    
    <div class="relative z-10">
      <h2 class="text-3xl md:text-5xl font-bold mb-6 text-white dark:text-mono-black">
        {config.title}
      </h2>
      <p class="text-lg md:text-xl mb-10 text-mono-light dark:text-mono-dark opacity-90 max-w-2xl mx-auto">
        {config.description}
      </p>
      
      <div class="flex flex-col sm:flex-row justify-center gap-4">
        <a href={config.ctaHref} class="bg-white dark:bg-mono-black text-mono-black dark:text-white px-8 py-4 rounded-xl font-bold hover:scale-105 transition-transform">
          {config.ctaText}
        </a>
        <a href={config.secondaryHref} class="border border-white/20 dark:border-mono-black/20 text-white dark:text-mono-black px-8 py-4 rounded-xl font-bold hover:bg-white/10 dark:hover:bg-black/5 transition-colors">
          {config.secondaryText}
        </a>
      </div>
    </div>
  </div>
</section>